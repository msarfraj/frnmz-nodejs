<!DOCTYPE html>
<html lang="en">
<% include header %>
<body>
<% include navbar %>
<div class="content">
<table id="transsumry" >
<tr><th>Email</th><th>Amount</th><th>Paid On</th></tr>
<%val.response.forEach(function(item) { %>
     <tr><td><%= item.email %></td><td><%= item.amount %></td><td><%= item.paidon.toDateString() %></td><tr>
    <% }); %>
    
 </table>   
    </div>
    <% include footer %>
</body>
</html>